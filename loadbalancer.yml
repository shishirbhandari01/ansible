---
- hosts: loadbalancer
  tasks: 
    - name: install epel-release
      yum:
        name: epel-release
        state: latest
    - name: install nginx 
      yum:  
        name: nginx  
        state: latest 
    - name: ensure nginx is started 
      service: name=nginx state=started enabled=yes
 
